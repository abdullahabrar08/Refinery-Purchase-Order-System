# Users Auth Service – use with docker-compose auth-db
NODE_ENV=development
PORT=4001

# Auth DB (PostgreSQL) – matches docker-compose auth-db
DB_HOST=localhost
DB_PORT=5432
DB_USER=pos_dev
DB_PASSWORD="pos_dev@123"
DATABASE=users_dev

# JWT
JWT_SECRET=your-jwt-secret-change-in-production
JWT_EXPIRE=24h
