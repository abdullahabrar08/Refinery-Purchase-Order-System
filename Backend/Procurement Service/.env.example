NODE_ENV=development
PORT=4003

# Procurement DB (PostgreSQL) – docker-compose procurement-db (host port 5433)
DB_HOST=localhost
DB_PORT=5433
DB_USER=pos_dev
DB_PASSWORD="pos_dev@123"
DATABASE=procurement_dev

# Must match Auth service
JWT_SECRET=users-auth-jwt-secret-change-in-production

# Catalog service base URL (for GET /items/:id)
CATALOG_SERVICE_URL=http://localhost:4002/catalog
